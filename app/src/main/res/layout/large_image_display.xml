<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:background="@android:color/transparent"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">
    <include
        android:id="@+id/tool_bar"
        layout="@layout/toolbar"
        android:fitsSystemWindows="true">
    </include>
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <in.co.madhur.chatbubblesdemo.utils.TouchImageView
            android:id="@+id/large_image_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginRight="10dip"
            android:layout_marginLeft="5dp"
            android:layout_marginTop="0dp"
            android:scaleType="centerCrop"
            android:gravity="right|center_vertical"
            android:background="@android:color/transparent"
            android:src="@drawable/pages_default_pic" />

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_alignParentBottom="true"
            android:background="@android:color/transparent"
            android:orientation="horizontal"
            android:layout_height="wrap_content">
            <TextView
                android:layout_width="100dp"
                android:layout_height="wrap_content"
                android:padding="10dp"
                android:layout_margin="5dp"
                android:gravity="center"
                android:id="@+id/page_upload_cancel_button"
                android:text="Cancel"
                android:textColor="#fff"
                android:textSize="18dp"
                android:visibility="gone"
                android:background="@drawable/border_rounded_dark"
                />
            <TextView
                android:layout_width="100dp"
                android:layout_height="wrap_content"
                android:layout_margin="5dp"
                android:gravity="center"
                android:padding="10dp"
                android:id="@+id/page_upload_save_button"
                android:text="Save"
                android:textColor="#fff"
                android:textSize="18dp"
                android:visibility="gone"
                android:background="@drawable/border_rounded_dark"
                />
        </LinearLayout>
    </RelativeLayout>

</LinearLayout>